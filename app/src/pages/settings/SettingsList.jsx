import React, { useState, useLayoutEffect, useEffect } from "react";
import {
    readConfigRequest,
    readConfigResponse,
    writeConfigRequest,
} from "secure-electron-store";

const Settings = () => {
    useLayoutEffect(() => {}, []);

    const handleSettigChange = () => {};

    return (
        <>
            <section className="section">{/* {tood} */}</section>
        </>
    );
};

export default Settings;
